interface:
  display_name: "Cargo"
  short_description: "Repo-specific patterns and workflows for cargo"
  default_prompt: "Use the cargo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true